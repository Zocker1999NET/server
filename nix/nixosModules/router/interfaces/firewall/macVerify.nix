# TODO feature: given known MAC IP pairs
# on ingress, we can enforce that known MACs only use known source IPs
# (breaking IPv6 privacy extension)
# further, we could block unknown MACs at all (breaking MAC randomization)
{ }
